// variables3.rs
// Make me compile! Execute the command `rustlings hint variables3` if you want a hint :)

fn main() {
    let x: i32 = 10; // Initialize `x` with a value, for example, 10.
    println!("Number {}", x);
}